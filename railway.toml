[build]
builder = "dockerfile"

[deploy]
startCommand = "sh -c 'uvicorn server.app:app --host 0.0.0.0 --port $PORT'"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
